"use client";

// The directive has to be in this file: Next checks it on app/global-error by
// filename, not on the module the default export comes from.
import GlobalErrorPage from "@brand/shared/components/global-error-page";

export default GlobalErrorPage;
